<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/assets/logo.PNG" />
    <title>
        <%= post.title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
    <style>
        html,
        body {
            height: 100%;
            /* Full height of the viewport */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
        }

        .main-container {
            flex: 1 0 auto;
            /* Flex grow, no flex-shrink, auto basis */
            padding: 20px;
            /* Adjust padding as needed */
            overflow: auto;
            /* Allows scrolling within the main container if needed */
        }

        footer {
            flex-shrink: 0;
            /* Footer won't shrink */
            height: 30px;
            /* Reduced height for the footer */
            line-height: 30px;
            /* Aligns text vertically */
            background-color: #f5f5f5;
            border-top: 1px solid #e5e5e5;
            width: 100%;
            text-align: center;
            /* Center the text horizontally */
        }
    </style>

</head>

<body style="background-color: white;">
    <%- include('navbar', { user: user }) %>
    <div class="ui container main-container"> <!-- Flex-grow to fill available space -->
        <h1><%= post.title %></h1>
        <p>Posted by <%= post.author %> on <%= new Date(post.date).toDateString() %></p>
        <div class="ui segment">
            <p><%= post.content %></p>
        </div>
    </div>
    <%- include('./footer') %>
</body>

</html>